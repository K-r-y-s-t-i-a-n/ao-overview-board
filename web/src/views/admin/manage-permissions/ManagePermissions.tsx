import { ViewWrapper } from '../../../components/core';
import { Title } from '@mantine/core';

const ManagePermissions = () => {
  return (
    <ViewWrapper>
      <Title mb={12}>Manage Roles & Permissions</Title>

      <div>ManagePermissions</div>
    </ViewWrapper>
  );
};

export default ManagePermissions;
